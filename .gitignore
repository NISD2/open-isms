/node_modules
.dist
# Match all .env variants (.env, .env.local, .env.vercel, .env.coolify,
# .env.production, .env.staging, etc.) except the committed .env.example
.env*
!.env.example
.next/
tsconfig.tsbuildinfo
google-service-account.json
.DS_Store
next-env.d.ts
eval-bsig-results.json
eval-panel-results.json
generated-guides.json
research/*.pdf
public/downloads/*.pdf
courses/**/pdf-book/*.aux
courses/**/pdf-book/*.log
courses/**/pdf-book/*.toc
courses/**/pdf-book/*.out
courses/**/pdf-book/*.ans

# Local Claude state
.claude/scheduled_tasks.lock

.vercel

# Business plan PDF build artifacts (regenerated via scripts/generate-business-plan-pdf.ts)
business/plan/rendered/


# Personal submission PDFs (BA GZ 04 contains AfA-Kundennummer)
business/correspondence/ihk-submission/

# Personal PDFs in correspondence (Eröffnungsbilanz, BA GZ forms with Kundennummer, etc.)
business/correspondence/**/*.pdf

# Meeting transcripts — private, not for version control
meeting-transcripts/

# Workspace package symlinks — bun creates per-package node_modules
packages/*/node_modules
apps/*/node_modules
packages/*/tsconfig.tsbuildinfo
apps/*/tsconfig.tsbuildinfo
